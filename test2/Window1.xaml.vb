Public Class Window1
    'Sub New()

    ' この呼び出しはデザイナーで必要です。
    '    InitializeComponent()

    '    ' InitializeComponent() 呼び出しの後で初期化を追加します。
    '    Dim vm As New ViewContents
    '    Dim vma As New ViewModelA
    '    Dim vmb As New ViewModelB

    '    Dim type = "A"
    '    Dim obj As Object

    '    If type = "A" Then
    '        vma.Content = vm
    '        obj = vma
    '    Else
    '        vmb.Content = vm
    '        obj = vmb
    '    End If

    '    Me.DataContext = obj
    'End Sub
End Class
