<Window x:Class="Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:test2"
        mc:Ignorable="d"
        Title="Window1" Height="500" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="5" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="5" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <StackPanel 
            Name="MainContent"
            Grid.Row="0" Grid.Column="0">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Hight=" />
                <TextBlock Text="{Binding ElementName=MainContent, Path=ActualHeight}" />
                <TextBlock Text=", " />
                <TextBlock Text="Width=" />
                <TextBlock Text="{Binding ElementName=MainContent, Path=ActualWidth}" />
            </StackPanel>
        </StackPanel>
        <StackPanel           
            Name="RightContent"
            Grid.Row="0" Grid.Column="2">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="RightWidth=" />
                <TextBlock Text="{Binding ElementName=RightContent, Path=ActualWidth}" />
            </StackPanel>
        </StackPanel>
        <GridSplitter
            x:Name="HorizontalSplitter"
            IsEnabled="{Binding IsHorizontalSplitterEnabled}"
            Grid.Row="0" Grid.RowSpan="2"
            Grid.Column="1"                                         
            ResizeDirection="Columns"                
            ResizeBehavior="PreviousAndNext"
            VerticalAlignment="Stretch"
            HorizontalAlignment="Stretch" />
        <GridSplitter
            x:Name="VerticalSplitter"                        
            IsEnabled="{Binding IsVerticalSplitterEnabled}"
            Grid.Column="0" Grid.ColumnSpan="2"
            Grid.Row="1"
            ResizeDirection="Rows"
            ResizeBehavior="PreviousAndNext"
            VerticalAlignment="Stretch"
            HorizontalAlignment="Stretch" />
        <StackPanel 
            Name="BottomContent"
            Grid.Row="2" Grid.Column="0">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="BottomHeight=" />
                <TextBlock Text="{Binding ElementName=BottomContent, Path=ActualHeight}" />
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
